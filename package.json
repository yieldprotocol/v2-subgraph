{
  "name": "yield-v2-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ yieldprotocol/v2-mainnet",
    "deploy-arbitrum": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ yieldprotocol/v2-arbitrum subgraph-arbitrum.yaml",
    "deploy-testnet": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ yieldprotocol/v2-kovan subgraph-kovan.yaml",
    "create-local": "graph create --node http://localhost:8020/ yieldprotocol/mainnet-staging",
    "remove-local": "graph remove --node http://localhost:8020/ yieldprotocol/mainnet-staging",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 yieldprotocol/mainnet-staging"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.2",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}
